# --- Local dev: use .env (python-dotenv). No .venv required for structure. ---
# --- Databricks: set DATABRICKS_SECRET_SCOPE and store secrets in that scope; config.py reads them. ---

# Database: leave unset or use SQLite for local dev (no DB server needed).
# run_ingest.py defaults to sqlite:///health.db if you don't pass --db.
# DATABASE_URL=sqlite:///health.db
# For Postgres (e.g. Databricks / production): set in .env or in Databricks secrets
# DATABASE_URL=postgresql://user:pass@host:5432/health_ghana

# Embeddings (identity dedup + append-to-DB: match new rows to existing orgs)
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536
# Only send close matches to the agent (cosine >= threshold, max N candidates) to save tokens
# EMBEDDING_SIMILARITY_THRESHOLD=0.82
# MAX_CANDIDATES_FOR_AGENT=3

# LLM for merge / same-or-new agent (store in Databricks secret scope in prod)
OPENAI_API_KEY=sk-...
LLM_MODEL=gpt-4o-mini

# Geocoding: set to 1 to fill lat/lon from addresses (Nominatim, 1 req/s). Off by default.
# GEOCODE_ENABLED=1

# Optional: only on Databricks. When set, config is read from secrets.get(scope, key) instead of .env
# DATABRICKS_SECRET_SCOPE=your-secret-scope
