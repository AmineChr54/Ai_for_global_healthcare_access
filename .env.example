# ─────────────────────────────────────────────────────────────────────────────
# Unified config: one place for API key and database path.
# Copy to .env and set values. Production (Databricks): set DATABRICKS_SECRET_SCOPE
# and store OPENAI_API_KEY and DATABASE_URL in that scope; .env is not loaded then.
# ─────────────────────────────────────────────────────────────────────────────

# Database (local default: SQLite in repo)
DATABASE_URL=sqlite:///ghana_dataset/health.db
# For Postgres (e.g. Databricks / production):
# DATABASE_URL=postgresql://user:pass@host:5432/health_ghana

# Embeddings (identity dedup + merge agent)
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536

# LLM for merge / same-or-new agent (store in Databricks secret scope in prod)
OPENAI_API_KEY=sk-...
LLM_MODEL=gpt-4o-mini

# Geocoding: set to 1 to fill lat/lon from addresses (Nominatim, 1 req/s). Off by default.
GEOCODE_ENABLED=1

# Optional: only on Databricks. When set, config is read from secrets.get(scope, key) instead of .env
# DATABRICKS_SECRET_SCOPE=your-secret-scope

# Frontend: API base URL. Leave unset for local dev (default http://localhost:8000).
# For Databricks / combined deploy set to empty so the app uses relative /api (proxied to backend).
# NEXT_PUBLIC_API_URL=
